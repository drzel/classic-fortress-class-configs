set sws mouse2            // (secondary fire)
bind mwheelup sw2         // (switch secondary to slot2)
bind mwheeldown sw3       // (switch secondary to slot3)
bind $sws +slot2          // defaults secondary weapon to slot2 when changing class
